# Minutes - 2026-04-20

## Branch

- Active branch: `upgrade/1.0.5`
- Current head: `7b1a53b` `Update docs for local sync workflow`

## TODOs Carried Forward

Source: [minute-20260419.md](/Users/sergevilleneuve/MyMacApps/MomRecette/minute-20260419.md) `Recommended Next Steps`

1. [Review] Add one stable simulator-visible sync folder strategy so export/import does not depend on hidden CoreSimulator storage paths.

   - Archon status: `review`
   - Archon task: `e0a8f076-22f1-4645-b137-e89cea56be10`
2. Verify reverse sync again on iPhone using the `Sync` entry inside recipe detail.
3. Decide whether to merge `upgrade/1.0.5` after one final smoke pass across:
   - Mac
   - iPhone
   - iPad
4. If paid Apple services become available later:
   - continue from the Core Data foundation
   - enable CloudKit properly
   - validate true Mac/iPhone/iPad live sync
### Yesterday TODOs (Verbatim)

1. Add one stable simulator-visible sync folder strategy so export/import does not depend on hidden CoreSimulator storage paths.
2. Verify reverse sync again on iPhone using `MomRecette-Sync-Latest.json` from the new `Sync` entry inside recipe detail.
3. Decide whether to merge `upgrade/1.0.5` after one final smoke pass across:
   - Mac
   - iPhone
   - iPad
4. If paid Apple services become available later:
   - continue from the Core Data foundation
   - enable CloudKit properly
   - validate true Mac/iPhone/iPad live sync

## Today’s Work

### 1. Incremental shared queue sync

- Extended the sync model beyond full package import/export.
- Added a shared incremental queue file:
  - `MomRecette-Sync-Queue.json`
- Each device now tracks:
  - remembered shared queue file
  - last applied queue sequence
  - pending local operations
- Queue operations now cover:
  - recipe upsert
  - recipe delete
  - grocery list replace
  - grocery list clear

### 2. Queue compaction and stale-device catch-up

- Added queue compaction so stale operations do not grow forever when newer ones supersede them.
- Preserved monotonic sequence numbers so slower devices can still catch up safely from the latest retained state.

### 3. Automatic shared-queue sync hooks

- Added automatic shared-queue sync trigger from app startup flow.
- Added startup notice behavior when a device knows the shared queue file but does not have a trusted checkpoint yet.
- Kept manual `Sync` screen actions in place for explicit operator control and recovery.

### 4. Sync UI expansion

- Extended the `Sync` screen to support:
  - choosing a shared queue
  - creating a new shared queue bootstrap file
  - synchronizing against the remembered queue
  - showing queue path and sync result summaries

### 5. Tests and sync coverage expansion

- Added focused test coverage for:
  - queue round-trip behavior
  - delete propagation
  - compaction behavior
  - last-sequence tracking
  - automatic sync startup notice
  - automatic sync pull from remembered queue

### 6. Platform governance note relevant to this repo

- Recorded a platform-level governance rule outside the app code:
  - web research informs decisions, but does not automatically become truth
  - important claims should be classified as `Observed`, `Decided`, `Implemented`, or `Validated`
  - policy and high-impact claims require approval/review evidence before being treated as `Validated`
- Saved this in shared Archon context and in platform doctrine so later agents can recover it.

### 7. Minutes inbox created in Archon

- Created a pinned Archon project:
  - `Minutes Inbox`
- Purpose:
  - collect unresolved items from daily minutes
  - preserve source traceability
  - let the Scrum Master promote selected items into real sprint work later
- Seeded the inbox with current unresolved `MomRecette` minute items from this file.

### 8. Minutes inbox automation and outcome write-back

- Added a canonical Synapse script to sync unresolved minute items into Archon:
  - `scripts/sync_minutes_inbox.py`
- Added a canonical Synapse script to write Archon task outcomes back into the source minutes file:
  - `scripts/writeback_minutes_inbox.py`
- Added a repo-local wrapper in `MomRecette` so the operator can run the full loop from this repo:
  - `bash scripts/reconcile_minutes.sh`
- Added a repo-local close-session command:
  - `bash scripts/close_session.sh`
- The write-back format now supports:
  - inline status marker on the original TODO item
  - Archon status line
  - Archon task ID line
- Important constraint:
  - generated Archon note lines are ignored by forward sync so a done item does not create a duplicate inbox task on the next sync.

### 9. Sprint 49 and shared-sync bootstrap implementation

- Created a new Archon sprint for the current sync push:
  - `Sprint 49 — Shared Sync Bootstrap Validation`
- Added and validated the first implementation slice:
  - canonical iCloud `SharedSync` folder resolution
  - stable active queue path
  - stable `MomRecette-Latest-Backup.json`
  - archive backup retention
  - per-device `known checkpoint` state instead of treating sequence `0` as automatically unknown
  - automatic canonical queue discovery before startup sync
  - visible bootstrap/recovery card inside `Sync`
  - explicit bootstrap action:
    - local backup first
    - restore latest shared backup
    - re-merge local-only recipes as local changes
    - drain newer queue operations after restore
- Also added foreground auto-sync trigger with throttling instead of launch-only behavior.

### 10. Sprint 49 smoke pass results and merge-readiness check

- Smoke validation reached the strongest currently available targets:
  - Mac Catalyst build succeeded
  - iPhone simulator build, install, and launch succeeded
  - iPad simulator install and launch succeeded
- Physical iPhone deployment did not complete because signing/provisioning is not aligned with the new iCloud shared-sync entitlements:
  - current profile does not include the new ubiquity/iCloud capability set
  - current profile does not include the currently selected device
- This means the code path is validated on Mac plus both simulator families, but the real-device Sprint 49 pass is still blocked by configuration outside the app code.
- Merge-readiness decision:
  - not fully ready to close Sprint 49 or declare hardware-validated merge readiness yet
  - conditionally ready for branch review from a code-quality standpoint once the provisioning profile is updated and one real-device sync smoke pass is rerun

### 11. Simulator-visible SharedSync path

- Added the next unblocked sync-operator improvement after the physical-device provisioning stop:
  - simulator builds now default to a stable shared root under `~/Documents/MomRecette-Simulator/SharedSync/`
- The goal is to let iPhone and iPad simulators share one visible queue and backup location without depending on hidden CoreSimulator app containers.
- Real devices still keep the canonical iCloud `SharedSync` location.
- Added an explicit override for simulator/local validation:
  - `MOMRECETTE_SHARED_SYNC_ROOT=/chemin/visible`
- Updated the `Sync` screen to explain when the app is using the simulator-local path instead of iCloud.

### 12. Bootstrap state-machine fix for empty devices

- Validated the operator mismatch seen on simulator:
  - queue remembered
  - no trusted checkpoint
  - no shared backup yet
- Fixed the startup notice and `Sync` warning state so MomRecette no longer tells the operator to initialize from a shared backup when no shared backup exists yet.
- Added an explicit “waiting for first shared backup” branch:
  - empty target devices are told to use the source device first
  - devices that already contain local data are told to publish the first shared backup
- Updated the queue action label so it reflects state better:
  - `Activer...`
  - `Reconfigurer...`
  - `Publier la premiere sauvegarde partagee`

### 13. Sync surface simplification

- Reduced the top-level SharedSync choice overload in the `Sync` screen.
- The shared-sync area now shows:
  - one primary next action based on device state
  - one bootstrap action only when a shared backup really exists
  - an advanced section for recovery and manual queue selection
- Manual JSON/package import and export actions remain available, but they no longer compete visually with the shared-sync setup flow.

### 14. Canonical MomRecette baseline

- Captured the established-project baseline for MomRecette in a repo-local artifact:
  - `docs/ARCHON_PROJECT_BASELINE.md`
- The baseline records the canonical attachment identifiers:
  - Archon project id
  - repo root
  - plan source
  - branch and captured head
  - bundle id
  - Xcode scheme
  - iCloud / SharedSync identifiers
- Added the local BMAD story for this baseline task:
  - `48-1-establish-canonical-baseline.md`

### 15. Canonical baseline attached to Archon

- Attached the same MomRecette baseline into the live Archon project as:
  - `MomRecette canonical project baseline`
- Archon baseline document id:
  - `c69694d0-643c-4936-879c-8e8c6dbd7649`
- Confirmed the canonical trace chain is already present and now linked from the baseline:
  - `idea_id`: `idea-momrecette-001`
  - `plan_id`: `plan-momrecette-001`
  - `story_id`: `48-1`
  - `archon_task_id`: `1d7e964e-245c-4d1d-a5a2-e172fddc4223`

### 16. Grocery-list story trace aligned

- Added the missing repo-local BMAD story for:
  - `48-2: Generate grocery list from selected recipe`
- Confirmed the implementation already satisfies the task scope:
  - recipe detail can create the list
  - ingredient names and quantities are preserved
  - the in-app checklist supports check/uncheck
  - the list persists locally through the store
- Added the canonical Archon trace row for the story:
  - `trace_id`: `bfaccf0a-dd15-49aa-8918-ed142388f551`
  - `archon_task_id`: `9dbec86e-cb7d-4c5f-9cc9-0184db2d8e5a`
- Validation:
  - `xcodebuild test -project MomRecette.xcodeproj -scheme MomRecette -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MomRecetteTests`
  - `53` tests passed, `2` skipped, `0` failures

### 17. SharedSync no-money path aligned

- Finalized the SharedSync surface reduction for the free/local workflow:
  - one primary SharedSync action at a time
  - bootstrap shown only when a shared backup really exists
  - recovery and queue-selection controls moved under `Options avancees SharedSync`
  - manual JSON/package actions kept below the SharedSync flow for explicit transfer workflows
- Updated the README so the no-money operator path is explicit:
  - SharedSync first on simulators
  - package transfer for explicit or physical-device handoff
- Full local validation still passes:
  - `53` tests passed
  - `0` skipped at that stage
  - `0` failures
- Promoted the story into Archon so Sprint 49 stays traceable:
  - `task_id`: `2688b9dd-cb68-4f64-a1b7-e43dcc117ee3`
  - `trace_id`: `f89071a7-8c2d-4fe8-b1a1-855f4d97bb39`

### 18. Default test suite no longer reports live-data skips

- Added the local BMAD story for this cleanup:
  - `49-5-remove-live-data-skips-from-default-test-suite.md`
- Identified the two skipped tests:
  - real local-library migration validation
  - real production-store canonicalization validation
- These were not flaky tests. They were opt-in manual validations against live MomRecette data under the operator's home directory.
- Fixed the default suite by converting them from `test...` cases into explicit manual validation helpers, so they no longer count as skipped in normal XCTest runs.
- Final validation after the cleanup:
  - `xcodebuild test -project MomRecette.xcodeproj -scheme MomRecette -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:MomRecetteTests`
  - `51` tests passed
  - `0` skipped
  - `0` failures

### 19. Repeatable two-simulator SharedSync smoke helper

- Added a repo-local helper for the no-money Sprint 49-2 path:
  - `bash scripts/simulator_sharedsync_smoke.sh`
- Added the local BMAD story:
  - `49-6-repeatable-two-simulator-sharedsync-smoke-helper.md`
- The helper now:
  - reuses one simulator build
  - boots a phone and an iPad simulator against the same shared root
  - installs the same app build on both
  - launches both with:
    - `MOMRECETTE_SHARED_SYNC_ROOT`
    - `MOMRECETTE_DISABLE_CLOUDKIT=1`
  - prints the exact SharedSync folder and the next manual smoke steps
- Validated on this Mac with:
  - phone: `iPhone 15 Plus`
  - tablet: `iPad Pro 11-inch (M5)`
  - shared root: `~/Documents/MomRecette-Simulator`
- Important note:
  - this validates the repeatable setup command itself
  - the actual operator smoke for `49-2` still needs to be completed inside the running simulators

## Key Validation Completed

- Earlier focused test runs in this branch passed for manual sync package export/import.
- New tests were added today for shared queue sync and automatic startup sync paths.
- Platform governance decision was saved in:
  - Archon shared context
  - Synapse BMAD artifacts
  - shared `FactoriesProjects` doctrine
- A dedicated Archon inbox project now exists for carried-forward minute items.
- The inbox-to-minutes reverse write-back was validated with a temporary file and real Archon task lifecycle.
- Focused iOS simulator validation now passes for the new sync/bootstrap path:
  - `49` tests passed
  - `2` skipped
  - `0` failures
- Broader Sprint 49 smoke validation now also succeeded on reachable runtime targets:
  - `xcodebuild build` succeeded for Mac Catalyst
  - app launched successfully on `iPhone 17` simulator
  - app launched successfully on `iPad (A16)` simulator
- Added deterministic simulator shared-sync path resolution tests:
  - explicit override wins
  - simulator fallback resolves to a stable Documents path
  - real-device runtime does not use the simulator fallback
- Added regression coverage for the empty-device bootstrap mismatch:
  - startup notice now points to the first shared backup instead of a nonexistent bootstrap action
- The default `MomRecetteTests` suite is now clean for normal local runs:
  - `51` tests passed
  - `0` skipped
  - `0` failures

## Current Known State

- Manual package sync still exists and remains usable.
- Shared queue sync is now the more capable path for incremental propagation between devices.
- App startup and foreground return now attempt automatic shared-queue sync when enough local sync state is known.
- If a device does not have a trusted checkpoint, startup stops short and points the operator to the bootstrap path instead of guessing.
- A new device can now bootstrap from the canonical shared backup and then continue from the queue.
- The unresolved minute items are now mirrored into the Archon `Minutes Inbox` project for SM triage.
- The former live-data migration/canonicalization checks remain available only as explicit manual helpers and no longer pollute the default XCTest suite.
- Physical iPhone verification is blocked by provisioning drift rather than a compile or runtime crash in the app.

## Open Issues / Risks

- No true live CloudKit sync yet.
- The simulator-visible SharedSync path is now defined, but still needs operator validation in a real two-simulator sync run.
- The new bootstrap flow has Mac and simulator coverage, but still needs a real physical-device smoke pass across at least two devices after provisioning is fixed.
- The current iOS provisioning profile does not include the required iCloud shared-sync entitlements or the connected test device.
- There is still unrelated local repo state present:
  - `AGENTS.md`
  - `.gitignore`
  - `question.txt`

## New TODOs

1. Update the Apple provisioning profile so it includes:
   - the new iCloud/ubiquity entitlements
   - the connected physical iPhone
2. Rerun Sprint 49 physical-device smoke validation:
   - create/use canonical iCloud `SharedSync`
   - bootstrap one fresh device from the shared backup
   - verify launch/foreground sync on at least two real devices
3. After the physical pass, confirm whether the current queue-based path is the release-candidate sync workflow.
## Recommended Next Steps

1. Fix provisioning/signing for the physical iPhone so the app can be installed with the new iCloud shared-sync entitlements.
2. Rerun Sprint 49 on real devices using the canonical iCloud `SharedSync` flow and one fresh-device bootstrap.
3. If that hardware pass succeeds, treat the branch as merge-ready; until then, keep the branch in review-ready but not hardware-validated state.
4. Keep carrying unresolved simulator/shared-folder issues into the next day until the operator workflow is stable.

### 20. First recipe audit recovery batch

- Paused the two-simulator operator smoke thread and moved to the next unblocked product cleanup task from `RECIPE_AUDIT.md`.
- Created BMAD story:
  - `49-7: First Recipe Audit Recovery Batch`
- Recovered `Poitrine de Dinde au Pamplemousse` in `momrecette.json` by moving leaked preparation text back into explicit steps and aligning the local working copy with the already-structured bundle version.
- Recovered `Salade de Haricots` in both `momrecette.json` and `Resources/momrecette_bundle.json` by splitting leaked preparation text out of ingredients and into explicit steps.
- Updated `RECIPE_AUDIT.md` so the completed batch and the next priority queue are explicit.
- Validation:
  - both JSON files load successfully
  - `Poitrine de Dinde au Pamplemousse` now has `13` ingredients and `7` steps in the working library
  - `Salade de Haricots` now has `12` ingredients and `7` steps in the working library
  - audit delta after the batch:
    - `steps_missing`: `45 -> 43`
    - `ingredient_quantity_embedded_in_name`: `153 -> 143`
- Current next recipe cleanup target:
  - `Makes 6 Naan`

### 21. Minutes Inbox sprint triage

- Paused the recipe-audit thread and switched to the `Minutes Inbox` project for SM triage.
- Inspected the current inbox snapshot and found `8` active tasks before reconciliation.
- Approved only the coherent no-money MomRecette sync-closure slice for sprint execution.
- Created and started Archon sprint:
  - `Sprint 1 — MomRecette Sync Closure Triage`
  - sprint id: `692484f2-8493-4e9c-a681-0a9afb32128e`
- Approved sprint scope:
  - `e0a8f076-22f1-4645-b137-e89cea56be10`
    - `MomRecette: Add one stable simulator-visible sync folder strategy...`
    - moved to `review`
  - `f225328c-ab0f-4429-9716-bf98f9321d80`
    - `MomRecette: Finish the new-device bootstrap path for shared sync`
    - moved to `review`
  - `ad62f28b-6da0-4c17-b4f9-ff92ec2aa779`
    - `MomRecette: Validate automatic sync behavior on real startup/foreground transitions with two devices`
    - moved to `doing`
  - `5e626091-8383-4462-b898-e6291dd6cb5e`
    - `MomRecette: Decide whether the current queue-based path is the release candidate sync workflow...`
    - kept in `todo` inside the sprint
- Explicitly excluded from the sprint:
  - physical-iPhone reverse-sync validation
  - final hardware merge decision
  - paid-Apple-services-later follow-up
- Archived stray inbox artifact:
  - `a31b894f-70cf-4ad7-b2d8-36d811fc1e79`
  - `WritebackTest: Keep another item open`
- Ran the canonical minutes reconciliation after the sprint update.
  - This pulled in `3` newer physical-device TODOs from today’s minutes, so the active inbox count is now `10`, but the sprint scope above remains the approved slice.

### 22. Two-device startup and foreground sync validation

- Continued the active sprint task:
  - `ad62f28b-6da0-4c17-b4f9-ff92ec2aa779`
  - `MomRecette: Validate automatic sync behavior on real startup/foreground transitions with two devices`
- Used the no-money simulator path with:
  - phone: `iPhone 15 Plus`
  - tablet: `iPad Pro 11-inch (M5)`
  - shared root: `~/Documents/MomRecette-Simulator/SharedSync`
- Reset the installed app on both simulators to get back to a truly clean state.
- Confirmed the clean post-launch baseline:
  - both simulators remembered the canonical queue path
  - both simulators had `hasResolvedQueueCheckpoint = false`
  - the shared queue folder was empty
- Startup proof:
  - preloaded one valid shared queue operation (`replaceGroceryList`, sequence `1`)
  - marked the tablet as an already-configured device with `lastAppliedQueueSequence = 0`
  - relaunched the tablet
  - observed:
    - `lastAppliedQueueSequence: 0 -> 1`
    - `lastSynchronizedAt` populated
    - `MomRecette-Latest-Backup.json` created in `SharedSync`
    - `momrecette-grocery-list.json` written with the imported payload
- Foreground proof:
  - appended a second queue operation (`replaceGroceryList`, sequence `2`)
  - waited past the automatic sync throttle window
  - reactivated the tablet
  - observed:
    - `lastAppliedQueueSequence: 1 -> 2`
    - shared backup updated to the second payload
    - local grocery list updated from `Pommes validation` to `Bananes validation`
- Important validation caveats:
  - `scripts/simulator_sharedsync_smoke.sh --clean` resets the shared folder, but not the installed app data, so stale simulator state can pollute a run unless the app is uninstalled first.
  - Simulator lifecycle is still flaky from CLI alone:
    - one helper rerun hit `Mach error -308`
    - one later reactivation attempt saw the simulator shut down unexpectedly
  - The sync logic validated cleanly, but the helper workflow still needs hardening before it can be treated as a deterministic one-command validation.

### 23. Queue-path release-candidate decision

- Evaluated the next sprint decision task:
  - `5e626091-8383-4462-b898-e6291dd6cb5e`
  - `MomRecette: Decide whether the current queue-based path is the release candidate sync workflow, or whether additional onboarding/recovery work is required first`
- Decision:
  - Keep the queue-based shared-sync path as the active sync direction.
  - Do **not** treat it as fully release-candidate ready across all devices yet.
- Why it is good enough to keep:
  - startup and foreground queue pulls now validate on a clean simulator target
  - shared backup creation and checkpoint advancement behave as designed
  - new-device/bootstrap and empty-device guardrails are in place
- Why it is not fully release-candidate ready yet:
  - the simulator helper does not yet guarantee a fresh app-state reset
  - simulator lifecycle from CLI is still flaky enough to weaken operator confidence
  - physical-device/iCloud validation remains blocked by provisioning drift
  - hardware merge readiness is still unresolved
- Practical conclusion:
  - the queue-based path remains the right sync workflow to continue hardening
  - additional onboarding/operational hardening is still required before calling it fully release-ready

### 24. Next sprint task pulled in

- The original sprint scope had no remaining executable item; all four earlier sprint tasks were already in `review`.
- Pulled the next coherent `Minutes Inbox` task into the active sprint:
  - `290d38f1-ccda-4ff2-a501-4a12dfb9bccb`
  - `MomRecette: Verify reverse sync again on iPhone using the Sync entry inside recipe detail`
  - moved to `doing`
- Verified by code inspection that the recipe-detail path still exists and is wired correctly:
  - `Views/Detail/RecipeDetailView.swift`
  - detail toolbar menu exposes `Sync`
  - selecting it opens the shared `ImportView` sheet
- Current gap:
  - this task is primarily operator validation, not a missing code-path fix
  - reverse-sync execution through the iPhone detail-entry flow still needs a real manual run

### 25. Hardened clean simulator smoke reset

- Skipped the manual reverse-sync task for now and promoted the next discovered executable fix into the same sprint:
  - `e2278e05-bed4-4bca-9a6e-dcf2d87f8560`
  - `MomRecette: Harden simulator SharedSync smoke helper so clean runs reset app state as well as the shared folder`
- Created BMAD story:
  - `49-8-harden-simulator-sharedsync-smoke-clean-reset.md`
- Updated `scripts/simulator_sharedsync_smoke.sh` so `--clean` now:
  - removes the `SharedSync` folder contents
  - terminates MomRecette on both selected simulators
  - uninstalls MomRecette from both selected simulators before reinstall
- Updated `scripts/README.md` to make the stronger clean behavior explicit.
- Validation:
  - `bash -n scripts/simulator_sharedsync_smoke.sh`
  - `bash scripts/simulator_sharedsync_smoke.sh --clean --skip-build`
  - after the run, both simulator app containers were fresh and only contained `momrecette-sync-state.json` under `Documents`
- Result:
  - clean simulator smoke runs now produce a real fresh-install baseline instead of carrying stale app containers forward

### 26. MomRecette sprint paused

- Operator direction changed to skip any further MomRecette app tasks for now.
- Checked the active `Minutes Inbox` sprint and confirmed:
  - every task in the sprint was MomRecette-specific
  - no non-MomRecette executable task remained inside the sprint
  - no task was still in `doing`
- Cancelled the Archon sprint to avoid leaving false active work in the system:
  - `Sprint 1 — MomRecette Sync Closure Triage`
  - sprint id: `692484f2-8493-4e9c-a681-0a9afb32128e`
  - new status: `cancelled`
- Parked MomRecette work now sitting in backlog or review:
  - reverse-sync validation from recipe detail
  - final merge decision
  - physical-device provisioning and smoke validation
  - later CloudKit follow-up
