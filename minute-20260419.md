# Minutes - 2026-04-19

## Branch

- Active branch: `upgrade/1.0.5`
- Remote branch head: `7b1a53b` `Update docs for local sync workflow`

## Today’s Work

### 1. Adaptive recipe workspace cleanup

- Kept the recipe detail default flow focused on `Ingredients`.
- Kept generated `Dish Photo` separate from `Recipe Card` so the card no longer replaces the hero dish photo.
- Moved `Recipe Card` viewing into the ingredient workflow and kept `Export` lighter.
- Added `calories per serving` support through model, edit flow, and metadata display.
- Added appearance mode support and app settings access from the Mac menu.
- Added Mac window size/position persistence.

### 2. Image generation flow hardening

- Improved the `Generate Image` sheet sizing and presentation for Mac Catalyst.
- Fixed broken share-sheet presentation in recipe detail.
- Added clearer error mapping and failure IDs in the image generation flow.
- Fixed preview wiring so `GenerateRecipeImageSheet` no longer depends on a missing `EnvironmentObject` in previews.

### 3. Local persistence and migration foundation

- Added a local Core Data persistence stack:
  - `RecipePersistentContainer.swift`
  - `RecipeCoreDataRepository.swift`
  - `RecipeMigrationCoordinator.swift`
  - `RecipeCoreDataImporter.swift`
- Added migration/backup planning from legacy local storage:
  - `momrecette.json`
  - `momrecette-grocery-list.json`
  - `RecipePhotos/`
  - `RecipeImages/`
- Kept the app local-first and CloudKit-ready, but not dependent on paid Apple services for the current workflow.

### 4. Free manual sync workflow

- Implemented manual package transfer between devices via the in-app `Sync` surface.
- Package now includes:
  - recipes
  - active grocery list
  - live photos
  - generated dish photos
  - generated recipe cards
- Import creates an automatic local backup before replacing the target device library.
- Added confirmation UI showing:
  - source device
  - export timestamp
  - counts
  - backup location
- Added success UI showing the exact backup file path.
- Renamed the surface from confusing import-only wording to `Sync`.
- Added `Sync` access from inside recipe detail via the `...` menu.

### 5. Stable sync file naming

- Changed sync export filename to:
  - `MomRecette-Sync-Latest.json`
- This avoids timestamp hunting between simulator devices.

### 6. Docs alignment

- Updated `README.md` to describe:
  - local Core Data persistence
  - manual package sync
  - current sync limitations
- Updated `docs/CLOUDKIT_SYNC_MIGRATION.md` to clarify:
  - current shipped state
  - manual sync as the live operator workflow
  - CloudKit as future target architecture

## Key Validation Completed

- Mac Catalyst build passed multiple times during the refactor.
- iPhone simulator build passed multiple times during the sync flow work.
- Focused tests passed for:
  - sync package round-trip preservation
  - sync package export payload and filename
  - migration and persistence slices added during the Core Data work
- Manual sync proven:
  - iPhone -> Mac import worked
  - Mac -> iPad import path worked after placing the package into simulator Files storage

## Commits Created Today

- `2c90c5c` `Add local sync foundation and manual package transfer`
- `7b1a53b` `Update docs for local sync workflow`

## Current Known State

- `upgrade/1.0.5` is pushed to remote at `7b1a53b`.
- Manual sync works, but it is not real-time sync.
- A fresh export still must be re-imported on the other device to propagate changes.
- Simulator Files access is still awkward and sometimes requires placing the exported package into simulator-visible Files storage.

## Known Gaps / Risks

- No true live CloudKit sync yet.
- No automatic cross-device propagation.
- iPhone/iPad simulator file picking is still constrained by the simulator Files app, not by the app’s package format.
- There is still an unrelated local modification in `AGENTS.md` that was not committed as part of this branch.

## Recommended Next Steps

1. Add one stable simulator-visible sync folder strategy so export/import does not depend on hidden CoreSimulator storage paths.
2. Verify reverse sync again on iPhone using `MomRecette-Sync-Latest.json` from the new `Sync` entry inside recipe detail.
3. Decide whether to merge `upgrade/1.0.5` after one final smoke pass across:
   - Mac
   - iPhone
   - iPad
4. If paid Apple services become available later:
   - continue from the Core Data foundation
   - enable CloudKit properly
   - validate true Mac/iPhone/iPad live sync

## Addendum - 2026-04-20

### Platform governance and doctrine

- Saved a shared Archon governance context:
  - `governance:validated-truth-and-web-review`
- Recorded the platform rule:
  - web research informs decisions, but does not automatically become truth
  - important claims must be classified as `Observed`, `Decided`, `Implemented`, or `Validated`
  - policy and other high-impact claims require approval or verification before they can be called `Validated`
- Saved BMAD proof in Synapse:
  - Epic `64` `FactoriesProjects Continuous Improvement And Learning Loop`
  - Story `64-1` `FactoriesProjects doctrine for pattern capture, evidence review, and durable memory`
- Enforced the rule in `FactoriesProjects/_playbooks/doctrine.yaml`:
  - repeated workflows must become reusable artifacts
  - repeated failures must add prevention mechanisms
  - important decisions must be externalized into durable shared artifacts
  - governance claims cannot be treated as validated without approval/review evidence
